@@ .. @@
   "scripts": {
     "dev": "EXPO_NO_TELEMETRY=1 expo start",
     "build:web": "expo export --platform web",
    "build:apk": "npx expo run:android --variant release --no-install",
    "build:apk-dev": "npx expo run:android --variant debug --no-install",
    "prebuild": "npx expo prebuild --platform android --clear",
     "lint": "expo lint",
     "android": "expo run:android",
     "ios": "expo run:ios"
   },